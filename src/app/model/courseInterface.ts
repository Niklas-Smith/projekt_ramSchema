
  // create interface courseInerface with the properties(values)

export interface CourseInterface {
  courseCode: string,
    subjectCode: string,
    level: string,
    progression: string,
    courseName: string,
    points: number,
    institutionCode: string,
    subject: string,
    syllabus: string

}
